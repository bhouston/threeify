{
  "name": "@threeify/math",
  "description": "Computer graphics oriented, High performance, tree-shakeable, TypeScript 3D vector math library.",
  "version": "4.0.3",
  "type": "module",
  "types": "./dist/index.d.ts",
  "module": "./dist/index.js",
  "source": "./src/index.ts",
  "scripts": {
    "build": "threeify-build src/index.ts --bundle",
    "dev": "threeify-build src/index.ts --bundle --watch",
    "types": "tsc --emitDeclarationOnly --outDir dist --watch"
  },
  "devDependencies": {
    "@threeify/build": "*"
  }
}
