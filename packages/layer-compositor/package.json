{
  "name": "@threeify/layer-compositor",
  "description": "Typescript 3D Library loosely based on three.js",
  "version": "4.0.2",
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "scripts": {
    "build": "glsl-transpiler  -r src -o src && tsc",
    "dev": "glsl-transpiler -r src -o src --watch & tsc --watch"
  },
  "dependencies": {
    "@threeify/core": "*",
    "@threeify/math": "*"
  },
  "devDependencies": {
    "@threeify/build": "*",
    "@threeify/glsl-transpiler": "*"
  }
}
