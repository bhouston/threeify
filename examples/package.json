{
  "name": "@threeify/examples",
  "version": "2.1.0",
  "type": "module",
  "main": "dist",
  "types": "dist",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "threeify-build src/**/index.ts --bundle --optimize",
    "dev": "threeify-build src/**/index.ts --bundle --serve",
    "types": "tsc --noEmit --watch"
  },
  "dependencies": {
    "@threeify/core": "*",
    "@threeify/math": "*",
    "@threeify/scene": "*"
  },
  "devDependencies": {
    "@threeify/build": "*"
  }
}
