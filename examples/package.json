{
  "name": "@threeify/examples",
  "version": "2.1.0",
  "type": "module",
  "main": "dist",
  "types": "dist",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "glsl-transpiler -r src -o src && bite build",
    "dev": "glsl-transpiler -r src -o src --watch & vite --watch"
  },
  "dependencies": {
    "@threeify/core": "*",
    "@threeify/math": "*",
    "@threeify/scene": "*"
  },
  "devDependencies": {
    "@threeify/build": "*",
    "vite": "^4.4.3",
    "vite-tsconfig-paths": "^4.2.0"
  }
}
