{
  "name": "@threeify/examples",
  "version": "2.1.0",
  "type": "module",
  "main": "dist",
  "types": "dist",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "builder src/**/index.ts --bundle --iife",
    "dev": "builder src/**/index.ts --bundle --serve --iife",
    "types": "tsc --noEmit --watch"
  },
  "dependencies": {
    "@threeify/core": "*",
    "@threeify/math": "*",
    "@threeify/scene": "*"
  },
  "devDependencies": {
    "builder": "*"
  }
}
