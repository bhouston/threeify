{
  "name": "@threeify/examples",
  "version": "2.1.0",
  "type": "module",
  "main": "dist",
  "types": "dist",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "glsl-transpiler && tsc --build",
    "watch": "glsl-transpiler --watch && tsc --build  --watch --preserveWatchOutput"
  },
  "dependencies": {
    "@threeify/core": "*",
    "@threeify/math": "*",
    "@threeify/scene": "*"
  }
}
